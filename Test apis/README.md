# Lara Tech Chatbot

A simple command-line chatbot built with the OpenAI-compatible API in this project.

## Run it

```bash
python main.py
```

Type a message and press Enter. Type `exit` or `quit` to leave the chat.
